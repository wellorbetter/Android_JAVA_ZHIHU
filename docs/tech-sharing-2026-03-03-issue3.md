# 技术分享（第 3 期）：如何快速做出“可联调闭环”后端

## 目标
不是一开始就上最重框架，而是先让客户端和后端跑通真实鉴权 + 真实 API。

## 方案
- 用 JDK 内置 `HttpServer` 快速起服务。
- 先落实最关键 3 类接口：Auth / Feed / Topic。
- 保持接口路径与客户端契约一致，保证后续平滑迁移。

## 收益
1. 立刻具备端到端联调能力。
2. 需求验证速度大幅提升。
3. 后续迁移 Spring Boot 时只替换实现，不改协议。
